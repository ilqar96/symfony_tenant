<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">Navbar</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('home') }}">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('posts') }}">Posts </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('members') }}">Company members </a>
            </li>

            <li class="nav-item">
                <span class="navbar-text selected-company"></span>
            </li>

        </ul>
        <ul class="navbar-nav ml-auto mr-3" >
            <li class="nav-item dropdown ml-auto">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    {{ app.user.email }}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a>
                    <a class="dropdown-item" href="{{ path('new_company') }}">Add new Company</a>
                    <a class="dropdown-item" href="{{ path('user_new') }}">Add new user</a>
                </div>
            </li>

            <li class="nav-item dropdown ml-auto">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Companys
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    {% for key , company in app.user.userTenants %}
                        <a class="dropdown-item company" data-tenant="{{ company.tenantId }}"  href="{{ path('change_tenant',{id:company.tenantId}) }}" >{{ loop.index }} Company</a>
                    {% endfor %}
                </div>
            </li>

        </ul>
    </div>
</nav>